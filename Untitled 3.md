асинхронная функция возвращает объект Promise в любом случае, даже если Promise не возвращается явно, JS оборачивает результат в объект Promise

await заставляет JS дождаться выполнения промиса
![[Pasted image 20250121133228.png]]