`getParsedExploreURLParams`
`fetchExploreData` - для получения датасета в режиме просмотра графика

 `const location = useLocation();`
 `const exploreUrlParams = getParsedExploreURLParams(location);`
 `fetchExploreData(exploreUrlParams, formData)
 